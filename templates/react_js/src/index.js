// index.js for React with JS
import React from 'react';
import ReactDOM from 'react-dom';

function App() {
  return (
    <div>
      <h1>Welcome to React with JS</h1>
    </div>
  );
}

ReactDOM.render(<App />, document.getElementById('root'));

